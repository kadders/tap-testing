[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v
markers =
    rrf: Duet/RepRapFirmware HTTP integration tests (read-only rr_connect + rr_model; TAP_RRF_BASE / milo.local)
